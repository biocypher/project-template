version: '3.3'

services:
  neo4j:
    build: .
    env_file:
      - docker_variables.env
    ports:
      - 7474:7474
      - 7687:7687
    volumes:
      - biocypher_neo4j_volume:/data
 #   restart: always

volumes:
  biocypher_neo4j_volume:
